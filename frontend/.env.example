# Copy to .env.local. Vite only exposes vars prefixed VITE_.
#
# The API base URL. There is no hardcoded localhost anywhere in src/ --
# every call goes through src/lib/api.js, which reads this.
VITE_API_BASE=http://127.0.0.1:5001/api
