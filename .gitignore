# Data artefacts — regenerated by the jobs, never committed
data/raw/
data/clean/
*.parquet

# Secrets
backend/.env
.env

# Logs (the stubbed mailer and alert runs write here)
logs/
*.log

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.ruff_cache/

# Node
node_modules/
frontend/dist/
frontend/.vite/

# Generated frontend snapshot (publish.py writes it; kept out of git so it
# cannot drift from the database silently)
frontend/public/snapshot.json

# Power BI export
powerbi/*.csv

# OS
.DS_Store
